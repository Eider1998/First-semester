#include<stdio.h>
void f(double a, double b, double c);
int main(void)
{
    double d, e, f, x;

    scanf("%lf %lf %lf", &d, &e, &f);
    f(x) = 0.0;
    return 0;
}
void f(double a, double b, double c)
{
    double x1, x2;

    f(x) = a * x * x + b * x + c;
    f(x) = 0;
    printf("%.2lf %.2lf\n", x1, x2);
}
